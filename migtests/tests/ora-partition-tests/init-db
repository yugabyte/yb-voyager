#!/usr/bin/env bash

set -e
set -x

source ${SCRIPTS}/functions.sh

echo "Initialising source database."
run_sqlplus ${SOURCE_DB_NAME} partitions_schema

echo "Initialising source database."
run_sqlplus ${SOURCE_DB_NAME} partitions_data
